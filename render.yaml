services:
  - type: web
    name: similia-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://YOUR-NETLIFY-SITE.netlify.app  # update after Netlify deploy
